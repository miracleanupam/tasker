<%= form_with(url: projects_url, local:true) do |f| %>
  <%= t.text_field :name, class: "form-control", placeholder: "Enter the name of the project" %>
  <%= t.submit "Submit", class: "btn btn-secondary" %>
<% end %>
